# MU OCTET — clone B (nyari) charter, 2026-08-01

Adversarial twin of clone A's FIELD build. Shared repo, worktrees only.
Sources: FIELD.md · AI-MU.md · 2026-08-01-field-adversary.md (§ = binding) ·
2026-08-01-field-recon.md. Style law: fragments + § pointers.

## Binding amendments (adversary doc → build LAW, not suggestion)
- ONE SPACE: d = W·H (128²=16384 V0) — hypervector IS a field slice → §1.
- ONE OP-SET: FFT-domain bind (=circulant matmul =wave step) · bundle (=+ =
  interference) · probe (=correlation =render/readout) → §1.
- plane ≠ module: plane = spatial marginal; propagation = fixed-kernel bind;
  render = reshape-probe. PNG seam = banned sin → §1 死.
- store = N×d matrix, RAM-resident + wired; SSD = cold journal ONLY, never
  demand-paged in a frame (~500× over budget measured math) → §2 死B, §4 死1.
- cadence split: field tick @120 (wave + resident probe + render) · learning/
  search AMORTIZED below render cadence, async → §4 死2.
- ring zero REDEFINED: sovereign single-address-space native runtime (wired
  mem + Metal + CoreML/ANE); aiwnios = REPL skin, NOT substrate. Literal EL1 +
  Apple accelerators = physically exclusive → §3.
- compiled ⊂ learned: circulant ⊂ dense; compile = exact point in weight
  space, learning descends FROM it → §2. Mechanize, don't cite.

## Octet map (branch · goal · acceptance)
1. mu-v0 · minimal MuZero loop MLX (repr/dyn/pred tiny nets, state = 16384
   field slice) · fused interactive tick ≤4-8 dispatches, measured <8.33ms,
   ≥100 train steps loss↓ → mu/BENCH.md.
2. tracr-bridge · circulant⊂dense PoC: bind≡circulant≡FFT (max-err), compiled
   exact recall (zero data), dense relax + descend from compiled point vs
   random init → bridge/PROOF.md.
3. ane-stream · CoreML fp16 conv mlprogram as BACKGROUND stream (not latency
   path): dispatch latency, throughput, GPU-interference both directions,
   ANE placement evidence → ane-stream/REPORT.md. Prior art: ane-spike/.
4. amortized-sched · two-cadence scheduler: 120Hz tick + async trainer +
   deadline back-pressure · PASS = p99 tick <8.33ms under live training →
   sched/REPORT.md.
5. adv-suite · attack clone A field core. T1 render=reshape-probe of state
   (non-field intermediate = FAIL) · T2 one-space (propagate symbol → probe
   recovers; shape/op mismatch = FAIL) · T3 godseed bit-identical @N=1000
   (ENTROPY.md) · T4 no SSD fault spikes in hot tick · T5 SNR tracks
   √(d/K). No runnable target yet → executable spec, PENDING-TARGET, never
   fake pass → adv/REPORT.md.
6. snr-cap · capacity curves: d∈{8192,16384,65536} × K sweep × atoms
   {gaussian, fourier, gabor} + N×d attention-store vs single-vector bundle
   at equal memory · explicit max-K@99% for d=16384 → snr/REPORT.md.
7. voice-plane · strike→vibrate→shape on the UNIFIED plane: FFT propagator
   (verified once vs 5-point stencil, deviation pasted), strike ripples,
   tone → Chladni nodal pattern, probe names the shape → voice/REPORT.md.
8. docs-mu-octet · this file.

## Conflicts (escalated, not silently forked)
C1 · plane-v0 0587c8d3 (clone A): separate 512² damped-wave stencil + PNG
render slice = the 死 branch of adversary doc §1 (two modules + PNG seam =
the exact banned sin). Recommendation: unify per amendments above — d=W·H,
propagator as FFT bind, render as reshape-probe. Clone A's code untouched;
decision belongs upstream.

## Status
Octet spec + task files staged (~/.cache/nyari-octet/t1-8.txt + launch.sh).
LAUNCH BLOCKED: clone B daemon token carries allowSummon:false (signed) —
native summon + `gaia summon` CLI both refuse. Unblock = relaunch clone B
with spawn rights OR run launch.sh from a summon-capable context. All lanes
UNVERIFIED until workers run.
