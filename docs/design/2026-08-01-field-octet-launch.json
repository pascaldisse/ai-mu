{
  "_meta": {
    "staged_by": "archon-clone A (nyari), 2026-08-01",
    "base": "branch field-unified @ 816c4c5e — skeleton compiles green (cargo build -p field)",
    "fire": "feed `whales` array directly to summon tool, ONE parallel wave",
    "routing": "terra ×4 code atoms · codex ×1 spike · sonnet ×3 pre-chewed",
    "contract": "docs/design/2026-08-01-field-contract.md — frozen signatures, ownership map, gates",
    "merge_wave2": "octopus merge field/* → field-unified → full cargo test -p field + bench + full-size parity"
  },
  "whales": [
    {
      "agent": "ghoul-terra",
      "task": "W1 FIELD CORE — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-core -b field/core field-unified && cd ~/projects/magic-crystal-field-core. THE SPEC = module docs in packages/field/src/fft.rs + atoms.rs (FROZEN signatures — replace todo!()/naive internals only, NEVER signatures) + docs/design/2026-08-01-field-contract.md. Your files ONLY: src/fft.rs internals, src/atoms.rs, tests/core.rs. Work: (1) fft.rs — iterative radix-2 Cooley-Tukey 2D FFT (rows then cols), power-of-two dims, precomputed twiddles, no external FFT crate, deterministic; (2) atoms.rs seeded_atom — unit-magnitude random-phase spectrum, phases from mix64(seed ^ mix64(bin_index)) ONLY, conjugate symmetry enforced (S[-k]=conj(S[k]), self-conjugate bins ±1 from seed) so output is real; (3) tests/core.rs: fft forward/inverse match lib.rs naive_dft2/naive_idft2 at 16x16+32x32 rel 1e-3 · inverse(forward(x)) roundtrip <=1e-4 · atom norm ~1 within 1e-3 · unbind(bind(a,x),a) recovers x rel 1e-3 at 128x128 · cross-seed |similarity|<0.1 at 128x128 · same seed = bit-identical atom (digest). Print fft 128x128 µs in a test (informational). LAWS: no rand crate, no clock in state, commit every stage, main untouched. GATES before done: cargo build -p field && cargo test -p field --test core — paste full output. RETURN: branch, commit SHAs, gate paste, fft 128² timing, UNVERIFIED list (expect none)."
    },
    {
      "agent": "ghoul-terra",
      "task": "W2 PLANE-AS-BIND — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-plane -b field/plane field-unified && cd ~/projects/magic-crystal-field-plane. THE SPEC = module doc in packages/field/src/plane.rs (FROZEN signatures — fill todo!() bodies only; K1/Lhat/a_prev math dictated there) + adversary verdict docs/research/2026-08-01-field-adversary.md §1. SOURCE PORT (read-only reference): ~/projects/magic-crystal-plane-v0/packages/plane/src/lib.rs (commit 0587c8d3) — port excite (sigma=2 r=8 displacement-strike jitter=1+0.1*seed_unit(seed,(x<<32)|y), V1 change: TORUS WRAP not edge clip), energy, to_gray, encode_png exactly; do NOT extend plane-v0 standalone (killed branch, C1 ruling). Your files ONLY: src/plane.rs internals, tests/plane_parity.rs. WaveKernel::new builds K1 spectrum ANALYTICALLY: K1(kx,ky)=(2-dd)+k*(2cos(2πkx/W)+2cos(2πky/H)-4), dd=damping*dt, k=(c*dt/dx)², a_prev=-(1-dd). wave_step = ifft(fft(cur)∘K1) + a_prev*prev. wave_step_reference = direct 5-point stencil with periodic wrap, same regrouped coefficients. Tests: PARITY max|fft_step-reference|<=1e-3 over 200 steps after one excite at 32x32 AND 64x64 (naive FFT in base is slow — keep dims small) · CFL stable() gate · energy monotone-decay with damping>0 · has_nonfinite stays false 500 steps · encode_png byte-identical across two same-seed runs. LAWS: no rand/clock, commit every stage, main untouched. GATES: cargo build -p field && cargo test -p field --test plane_parity — paste output. RETURN: branch, SHAs, gate paste, max parity error observed, UNVERIFIED list (full-size 128²/512² parity deferred to post-merge fast FFT — mark it)."
    },
    {
      "agent": "ghoul-terra",
      "task": "W3 COLD JOURNAL + STORE TESTS — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-journal -b field/journal field-unified && cd ~/projects/magic-crystal-field-journal. THE SPEC = module doc in packages/field/src/journal.rs (FROZEN: Op enum, Journal signatures, binary format v1 dictated in doc — magic FLDJ, version, cfg, params as f32-BIT-PATTERNS u32, n_slots, tag u8+payload LE, truncation=io error) + store.rs doc (store is FROZEN+working, do not edit src/store.rs). Your files ONLY: src/journal.rs internals, tests/store_journal.rs. Journal laws: append() buffers RAM only (NEVER disk — hot-path safe), flush() explicit off-cadence, create() writes header immediately. Tests: (a) store write/read roundtrip bit-exact · probe returns top-k sorted desc with deterministic tie-break lower-slot-first · probe skips reserved slots 0/1; (b) journal: every Op variant serialize→file→read_all roundtrip == PartialEq AND bit-exact for f32 bits · header roundtrip (cfg/params/n_slots) · truncated file → Err · append does not touch disk (file size unchanged until flush — assert via metadata) · two identical op sequences → byte-identical journal files. LAWS: no rand/clock, commit every stage, main untouched, never /tmp (test files under target/, deterministic names, clean up). GATES: cargo build -p field && cargo test -p field --test store_journal — paste output. RETURN: branch, SHAs, gate paste, format v1 byte-layout table, UNVERIFIED list."
    },
    {
      "agent": "ghoul-codex",
      "task": "W4 METAL/MLX SPIKE (measurement only, no Rust) — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e. Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-spike -b field/spike field-unified && cd ~/projects/magic-crystal-field-spike. Your files ONLY: tools/field-spike-mlx/* + docs/research/2026-08-01-field-metal-spike.md. GOAL: real numbers on THIS machine (Apple Silicon M1 Pro) for the 120fps budget (8.33ms/frame), per adversary verdict §4 + recon doc docs/research/2026-08-01-field-recon.md §2. Write tools/field-spike-mlx/spike.py: (1) bind = mx.fft.fft2 → complex multiply → ifft2 at 128² and 512² fp32; (2) probe = matmul key(1×d) @ store(N×d).T for N=4096 and N=16384, d=16384; (3) fused tick = bind+bind+probe under mx.compile; warmup 20 iters, measure 200, report mean/p50/p99 ms + PASS/FAIL vs 8.33ms; numpy CPU baseline for (1)+(2); deterministic seeds (mx.random.seed(0)); dump results JSON next to script + write the md report (device info, mlx/python versions, tables, verdict: does GPU tick fit the budget, headroom factor). Env: python3 -m venv .venv inside tools/field-spike-mlx/ + pip install mlx numpy (NEVER /tmp, never global). If install/run fails: commit script + exact error text, mark UNVERIFIED — do not fake numbers (testing law: measured or UNVERIFIED, nothing between). LAWS: commit every stage, main untouched, no Rust files touched. GATES: python3 tools/field-spike-mlx/spike.py runs end-to-end — paste stdout. RETURN: branch, SHAs, full numbers table, verdict line, UNVERIFIED list."
    },
    {
      "agent": "ghoul-sonnet",
      "task": "W5 DETERMINISM GATES — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-determ -b field/determ field-unified && cd ~/projects/magic-crystal-field-determ. THE SPEC = frozen signatures in packages/field/src/{world,journal,plane,atoms,store}.rs + lib.rs (digest/fnv1a/mix64) + docs/design/2026-08-01-field-contract.md. Your file ONLY: tests/determinism.rs. Write the ultradeterminism gate suite against the FROZEN contract (ENTROPY.md law: state = f(seed), bit-exact): (1) two Worlds, same cfg/params/n_slots, identical op sequence (SeedAtom ×8, Excite ×3, Step{100}, Bind, Bundle) applied live → world.digest() bit-identical; (2) journal replay: record same ops via Journal::create/append/flush → read_all → World::replay → digest == live world digest; (3) render_png bytes identical across the two runs; (4) digests DIFFER when seed differs (anti-vacuous sanity); (5) Slice::digest stable: same seeded_atom seed → same digest, twice. Use FieldConfig 32x32 or 64x64 (base FFT is naive/slow) + n_slots 16. KNOWN: deps have todo!() bodies until merge — deliverable = logically-correct tests that COMPILE; runtime green at merge. GATES: cargo build -p field --tests green — paste output; cargo test -p field --test determinism attempt: paste result, todo-panics expected → mark runtime UNVERIFIED-until-merge explicitly. LAWS: no rand/clock, deterministic file names under target/ for journal test (never /tmp), commit every stage, main untouched. RETURN: branch, SHAs, build-gate paste, UNVERIFIED list."
    },
    {
      "agent": "ghoul-terra",
      "task": "W6 WORLD + BENCH HARNESS — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-world -b field/world field-unified && cd ~/projects/magic-crystal-field-world. THE SPEC = module docs in packages/field/src/world.rs + src/bin/bench.rs (FROZEN signatures — fill todo!() bodies only) + journal.rs Op semantics table + store.rs slot convention (0=cur, 1=prev, RESERVED_SLOTS=2). Your files ONLY: src/world.rs internals, src/bin/bench.rs, optionally tests/world.rs. World::apply semantics: SeedAtom→store.write(slot, atoms::seeded_atom) · Excite→plane::excite on slots 0/1 (read rows→Slice, mutate, write back) · Step{count}→count× tick · Bind→store[dst]=ops::bind(store[a],store[b]) · Bundle→store[dst]=ops::bundle(srcs rows) · WriteRaw→f32::from_bits row write. tick() = plane::wave_step(cur=slot0, prev=slot1) → slot1←old cur, slot0←next, step_index+=1; HOT PATH: preallocate scratch where possible, zero I/O, zero journal calls. bench.rs per its module doc: argv flags with defaults (--width 128 --height 128 --slots 4096 --probe-keys 1 --frames 1000 --render-every 12; hand-rolled arg parse, no clap), loop = tick + store.probe + render every Nth, report ms/frame mean/p50/p99 + steps/s + PASS/FAIL vs 8.33ms. Instant allowed in bench harness only (never in state). KNOWN: plane.rs/atoms.rs are todo!() in your tree until merge — gate = compile-green + logically correct; runtime bench = post-merge, mark UNVERIFIED. Write tests/world.rs exercising apply/tick/replay digests (runs green post-merge). GATES: cargo build -p field && cargo build -p field --tests green — paste. LAWS: no rand/clock in state, commit every stage, main untouched. RETURN: branch, SHAs, gate paste, UNVERIFIED list."
    },
    {
      "agent": "ghoul-sonnet",
      "task": "W7 FIELD.md AMENDMENTS + HANDOFF LOG — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e. Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-docs -b field/docs field-unified && cd ~/projects/magic-crystal-field-docs. Your files ONLY: FIELD.md (append one new section, touch nothing above it), HANDOFF.md (one log entry). READ FIRST: FIELD.md + docs/research/2026-08-01-field-adversary.md (the verdict IS law) + docs/design/2026-08-01-field-contract.md. Append to FIELD.md a section '## V1 Amendments (08-01, adversary-forced — C1 ruling: verdict is law)' in the decree's own telegraphic voice (fragments, arrows, no prose bloat), covering EXACTLY: (1) ONE space unified NOW — d = W·H flattened grid (default 128², param); hypervector IS a field slice; one op-set FFT-domain: bind=circular conv · bundle=superposition · probe=correlation · render=reshape probe; (2) plane = fixed-kernel bind on the TORUS — plane-v0 (0587c8d3) = killed branch as standalone module, ported into packages/field/src/plane.rs; PNG = debug probe, not a render seam; (3) store = N×d RAM-RESIDENT matrix; SSD = cold journal ONLY (append-only, bit-exact replay); mmap-swap inside the frame loop BANNED (~500× over budget, measured physics); (4) ring zero REDEFINED = single-address-space sovereign RUNTIME: one wired native process, QoS user-interactive, direct Metal + CoreML/ANE; literal EL1 impossible on Apple Silicon with accelerators (Metal+ANE XOR bare metal); aiwnios = REPL/soul skin ON the runtime, not substrate under it; (5) cadence split LAW: 120fps floor = interactive field tick (wave+resident probe+render); learning/planning amortized async below cadence — no per-frame MCTS; (6) corpses kept, one line each, from adversary §Corpses. Then HANDOFF.md: dated entry — V1 skeleton landed on field-unified @816c4c5e, octet wave launched, contract doc pointer. GATES: none (docs); confirm git diff touches only FIELD.md+HANDOFF.md — paste git diff --stat. LAWS: commit every stage, main untouched, style law telegraphic. RETURN: branch, SHAs, diff-stat paste, the appended section text."
    },
    {
      "agent": "ghoul-sonnet",
      "task": "W8 CAPACITY/SNR CHARACTERIZATION — repo ~/projects/magic-crystal, base branch field-unified @816c4c5e (compiles green). Setup: git -C ~/projects/magic-crystal worktree add ~/projects/magic-crystal-field-capacity -b field/capacity field-unified && cd ~/projects/magic-crystal-field-capacity. Your files ONLY: tests/capacity.rs + docs/research/2026-08-01-field-capacity.md. CONTEXT: adversary verdict §2 — bundling K items into d dims degrades SNR ~ sqrt(d/K); the store is N×d BECAUSE one vector can't hold a world. Your job: measure it in THIS substrate. atoms.rs is todo!() in your tree — implement a TEST-LOCAL atom helper inside tests/capacity.rs (do NOT touch src/): unit-magnitude random-phase spectrum via mix64 phases with conjugate symmetry, materialized through lib.rs naive_idft2 (real output; follow the spec in src/atoms.rs doc comment). Use ops::{bundle, similarity} (working in base). At cfg 64x64 (d=4096): for K in {1,4,16,64,256}: bundle K distinct seeded atoms, measure similarity(bundle, member) for all members and similarity(bundle, distractor) for 256 fresh distractor atoms; compute mean member sim, mean |distractor| sim, separation margin, retrieval accuracy (member sim > max distractor sim). Tests assert: accuracy 100% at K<=16, >=95% at K<=64, member/distractor separation shrinks consistent with ~1/sqrt(K) (fit within factor 2). Doc: table (K, member μ, distractor μ, margin, accuracy), the sqrt(d/K) fit, extrapolation note for d=16384 (128²) + rec for store slot budget. Determinism: all seeds fixed constants. GATES: cargo build -p field && cargo test -p field --test capacity — paste full output (runs TODAY: no todo-deps). LAWS: no rand crate, commit every stage, main untouched. RETURN: branch, SHAs, gate paste, the table, UNVERIFIED list (expect none)."
    }
  ]
}
