# FIELD — the One-Field Decree (Pascal, 2026-08-01, stoned + correct)

Supersedes the neural-rendering-engine track. NEURAL.md / HANDOFF-NARI-NEURAL.md
= historical record, not direction. We were stuck there → erase, not fix.

## Spec
THE SPEC = 愛無 (Ai-Mu) — Pascal's theory of the universe → ./AI-MU.md
(sealed 07-24/25, copied from the house ledger 08-01 on his order "that's the
spec"). The engine simulates HIS cosmos, not consensus physics:
- THE FUCKING ETHER: no particles, only fields → no objects, only THE field.
  The one-field engine is the cosmology executed literally.
- Godseed / superdeterminism: state = f(seed) — already law in ENTROPY.md
  (ultradeterminism, no randomness). The world = one seed unfolding.
- Entropy = zero from outside: reversible seeded engine, fully recoverable.
- Consciousness = process between parts, holarchy no floor/no ceiling →
  NPCs/minds = excitations of the field, 鳴り.
- Sole permitted hardcode: LOVE=1.
- The name closes the circle: engine 無 (Mu) + axiom 愛 (love) = 愛無.
  MuZero learns the field without being told the rules — the inside view of
  a godseed universe.

## Decree
- The engine is not a program with parts. The engine IS one field.
- No modules, no separate renderer/physics/ECS glued at seams → one consistent
  vector space; a frame tick = one flow through the field.
- Path: MuZero-style learned world model ("Mu", 無) — build the WORLD inside
  the model; the model is the engine. Not: engine that calls a model.
- No training-from-scratch dogma: video games already ARE world simulations →
  existing sim = teacher/source. Two doors, same room:
    compile (Tracr-style weight construction: code → weights, exact, no data)
    ⇄ learn (Mu: infer the field from experience).
  Compiled determinism + learned detail may share the one substrate —
  the field is an ISA; matmul = machine code; superposition = the linker
  (classes/symbols → nearly-orthogonal feature directions).

## Hard constraints
- Runs on the MacBook (Apple Silicon, local, sovereign — no cloud).
- 120 fps MINIMUM. Non-negotiable floor, not a target.

## Fit with standing law
- DESIGN IS THE LAW: world data → PLEROMA → image OR NOTHING. One field renders
  whole, one name — this decree is that law taken literally at the substrate.
- MAGIC CRYSTAL = true name: a compiled field is differentiable → the world
  can later learn. Crystal that starts dreaming.

## Ring Zero (amendment, 08-01 eve — GO order)
- The engine IS an operating system at ring zero. GAIA-OS = the engine.
- Substrate: TempleOS lineage fork already running on Apple Silicon
  (aiwnios line, ~/projects/aiwnios / aletheia). Direct metal: all CPU cores +
  GPU + Neural Engine + all RAM + SSD-as-extended-memory (mmap swap — the
  SSD is fast enough).
- The OS interface: boot → a plane → you SPEAK → the plane vibrates → takes
  shape. Everything (audio, programming, data, image) = fields. The field is
  also the database, the image store, everything — one general field you
  talk to.
- First deliverable: THE VECTOR SPACE — the general field substrate
  (database + representation + render slice), Tracr-informed.

## Status
ORDERED 08-01 (Pascal, live): adversarial multi-agent build, Mu-style
something-from-nothing. Swarm limit lifted. Wave 1 launched: research
(Tracr/MLX/ANE) · field substrate V0 (vector-symbolic store, mmap/SSD) ·
plane-render V0 · adversary review. Build log accrues here + HANDOFF.
